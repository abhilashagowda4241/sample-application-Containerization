apiVersion: v1
kind: Service
metadata:
  name: sample-app-service
  namespace: team-alpha
spec:
  selector:
    app: sample-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: NodePort # Use LoadBalancer if you want an external AWS ELB URL
